import { } from '../constants/actionTypes';

export default function (state, action) {
  switch (action.type) {
    case (2 === 1): {
      return state;
    }
    default: {
      return state;
    }
  }
}
